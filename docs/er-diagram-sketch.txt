erDiagram

    USER {
        int id
        string username
        string password
        string email
        datetime date_joined
    }

    STAFF_PROFILE {
        int id
        string role // admin, physio, receptionist
        string phone
        int user_id FK
    }

    PATIENT {
        int id
        string first_name
        string last_name
        date dob
        string email
        string phone
        text medical_notes
    }

    APPOINTMENT {
        int id
        int patient_id FK
        int staff_id FK
        datetime start_time
        datetime end_time
        string status // scheduled, completed, cancelled, no-show
        text notes
    }

    INVOICE {
        int id
        int patient_id FK
        float amount
        date issue_date
        string status // paid, pending, overdue
        text description
    }

    INVENTORY_ITEM {
        int id
        string name
        int quantity
        int reorder_threshold
        string unit
        text notes
    }

    AUDIT_LOG {
        int id
        int user_id
        string action
        string entity
        int entity_id
        datetime timestamp
    }

    USER ||--o{ STAFF_PROFILE : "has"
    STAFF_PROFILE ||--o{ APPOINTMENT : "assigned to"
    PATIENT ||--o{ APPOINTMENT : "books"
    PATIENT ||--o{ INVOICE : "billed"
    USER ||--o{ AUDIT_LOG : "performs"
